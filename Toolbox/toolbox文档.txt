当前目录下有几个.command 文件，做成一个多合一的工具箱，单界面的Mac 的app，
界面左侧是4个脚本的名字，右侧上面是文本框，输入原来links.txt的内容，
下面是真实的终端，最下面分别是取消、确认按钮，默认值是确认按钮。
右边两个框都可以放到最大，有上下拉动的条；两框中间有条线，可以上下拉动，
调整框的大小。左侧与右侧之间也有条线，也可左右拉动，调整大小。
我的需求是：“GUI 外壳 + 内嵌真实终端+ 现有 shell 脚本原样复用”。
读取几个.command脚本的内容，看看我的需求是否能实现，用什么工具设计和实现？
还可以怎么优化得更好？

1. 完全本地自用
2. 完全打包成独立app，不需要额外安装任何依赖 
3. “取消”改成“停止”，“确认”改成“开始” 
4. 右边的两个框各增加一个放大到最大的符号，点击放大的最大，
 且右边都加滑动条，可以滑动看全部内容。 
5. 选Xcode + SwiftUI + PTY（真实终端）方案，

先在当前目录新建一个当前项目的文件夹，在里面给出一个骨架，剩下的我用Xcode修改。
