
1.Toolbox/修改50.txt 的这些没完成，继续修改：
      - 第1条的“1）把提示对齐”

2. 每日分配
      - 调整OCR识别结果面板的展示顺序，与报名截图中的顺序保持一致，终端的分配结果展示也用这个顺序
      - 打开“配置”，所见如图 Toolbox/每日分配-增加名单的空间.png，可以把红框的位置利用上，展示更多的名单
            - “名单中没有的人...新增姓名。” -> “注意：名单中没有的，不会给他分配任务。共 N 人。”，调整名单后，N 随着改变。

3. 批量打开链接
      - 打开“帮助”
            - "在上方输入多行链接内容。" -> "把链接放到文本框中。"
            - 删除“帮助”中的“批量打开链接”
            - 删除“配置说明：
                  - `BATCH_SIZE=10`
                  每批打开多少个链接后暂停，等待你按回车继续。”
      - 打开“配置”
            - “批量打开链接” -> "每次打开几个链接"
            - 删除“设置每批...重复链接。”
            - 点“保存”后，文本框变窄、终端变高，比例失衡，需要恢复之前的比例
      
4. 图片批量下载
      - 打开“帮助”，把“1. 在上方输入原来 `links.txt` 的内容。”替换为“1. 把链接放到文本框中。”
            - 删除“帮助”中的“图片批量下载”
            - “11 https://example.com/b.png” -> "11 https://example.com/b.png (图片重命名为 11.png)"
            - "https://example.com/c.png custom-name" -> "https://example.com/c.png custom-name (图片重命名为 custom-name.png)"
            - 删除“配置说明：
                  - `OUTPUT_DIR=~/Downloads`
                  默认下载目录。未填写时使用 `~/Downloads`。
                  ”

5. 图片拼接
      - 上下 两两 拼接、左右 两两 拼接，这2个的小图标反了，互换一下。目前3个功能的顺序不变。
      - 上下 三三 拼接，现在的图标是左右 三三 拼接的，弄错了，换一个对的
      - 这3个选项目前的位置是：上下的顶部+左右居中，改成：上下的中间+左右居中

6. AI&答题卡-检查 
      - “👉 已生成：Desktop/AI_check.xlsx ✅”中的“Desktop”不要写死，是AI_check.xlsx当前所在的文件夹

7. 上次修改出现“• 包内 PyInstaller 可执行文件不能在打包后再 lipo -thin，因为 PyInstaller 的归档附在 Mach-O 后面，瘦身会破坏归档。我已经捕获到这个问
   题，接下来只瘦身 Playwright node，保留 PyInstaller 启动器 universal2，然后重新打包并复验。”，判断该内容是否需要用来更新Toolbox/AGENTS.md及其相关文档，
   需要的话，就更新一下。如果本次修改后，需要更新相关文档，也更新一下。

8. 完全改好之后打一版 apple 包 + intel 包，参考：Toolbox/AGENTS.md