1. 批量打开链接
    - 修改提示，增加一个空行
    “
    # 每次打开10个链接，可以改成其他值
    BATCH_SIZE=10
    # 先去重，再打开链接；1 -> 去重，0 -> 不去重，可修改
    DEDUPE_LINKS=1
    ”
    需要改成：
    “
    # 每次打开10个链接，可以改成其他值
    BATCH_SIZE=10

    # 先去重，再打开链接；1 -> 去重，0 -> 不去重，可修改
    DEDUPE_LINKS=1
    ”
2. app的图标替换为：Toolbox/icon-黑820.png
