1. 批量打开链接
    - 用Toolbox_AppleSilicon_v6.zip测试，上次的“配置”修改失败，现在点“配置”，仍然只看见：
    “
    # 每次打开几个链接，直接改下面的数字
    BATCH_SIZE=10
    ”
    需要改成：
    “
    # 每次打开10个链接，可以改成其他值
    BATCH_SIZE=10
    # 先去重，再打开链接；1 -> 去重，0 -> 不去重，可修改
    DEDUPE_LINKS=1
    ”
    检查原因，修改好
2. 打开该app，dock栏该app的图标比其他的大一圈，检查原因并修改
